/**:
  ros__parameters:
    icp.max_iter: 100
    icp.translation_tolerance: 0.001
    icp.rotation_tolerance: 0.001
    icp.max_corr_distance: 0.1
    icp.voxel_grid_size: 0.03

    ekf.is_degree_imu: true

    frame_id: "map"
    child_frame_id: "base_link"

    near_lidar_threshold: 0.5